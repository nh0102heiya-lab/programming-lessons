markdown: kramdown
highlighter: rouge

collections:
  rust-from-zero:
    output: true

defaults:
  - scope:
      type: rust-from-zero
    values:
      layout: default
      permalink: /rust-from-zero/:path/
